# Port targets — `similar-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `similar` that codex-rs uses, and therefore the minimum subset that `similar-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-138] (rank `119`, tier `direct`, status `To Do`)

**Cargo package name**: `similar`
**Rust module name (in `use` statements)**: `similar`

## Summary

- **1** distinct symbols imported by codex-rs
- **5** total `use` statements
- **5** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `similar::TextDiff`

- **5 `use` site(s)** across **4 codex member crate(s)**: `codex-app-server-protocol`, `codex-apply-patch`, `codex-core`, `codex-git-utils`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/tests/schema_fixtures.rs:6` (`codex-app-server-protocol`) — `use similar::TextDiff`
- `apply-patch/src/lib.rs:24` (`codex-apply-patch`) — `use similar::TextDiff`
- `core/src/config/schema_tests.rs:6` (`codex-core`) — `use similar::TextDiff`
- `core/src/config_lock.rs:10` (`codex-core`) — `use similar::TextDiff`
- `git-utils/src/baseline.rs:7` (`codex-git-utils`) — `use similar::TextDiff`
